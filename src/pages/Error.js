import React from 'react';

const Error = () => {
    return (
        <div>
            <h1 style={{color: 'red'}}>
                Such page  is non-existent
            </h1>
        </div>
    );
};

export default Error;